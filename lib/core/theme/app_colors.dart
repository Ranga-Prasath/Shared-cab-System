// -- Shared Cab System --
// Design system: App Colors

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary brand
  static const primary = Color(0xFF6C63FF);
  static const primaryDark = Color(0xFF4A42D4);

  // Accent
  static const accent = Color(0xFF00D9A6);

  // Night Mode
  static const nightPrimary = Color(0xFF1A1A2E);
  static const nightSurface = Color(0xFF16213E);
  static const nightAccent = Color(0xFFE94560);
  static const nightMoon = Color(0xFFF5C518);

  // Semantic
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const danger = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);

  // Neutrals
  static const backgroundLight = Color(0xFFF8FAFC);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF1E293B);
  static const textSecondary = Color(0xFF64748B);
  static const textMuted = Color(0xFF94A3B8);
  static const divider = Color(0xFFE2E8F0);

  // Dark mode neutrals
  static const textPrimaryDark = Color(0xFFF1F5F9);
  static const textSecondaryDark = Color(0xFF94A3B8);

  // Savings gradient
  static const savingsGradientStart = Color(0xFF22C55E);
  static const savingsGradientEnd = Color(0xFF06B6D4);

  // Safety gradient
  static const safetyGradientStart = Color(0xFFE94560);
  static const safetyGradientEnd = Color(0xFFF5C518);
}
